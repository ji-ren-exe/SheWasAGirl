

## Codely Structured Memories

undefined
- [2026-08-29 20:59:01] 任务UI黑板样式已被废弃（2026-08-29 改版）：原 Blackboard.png/ChalkLine.png 黑板+粉笔下划线样式已移除，改为纯深色半透明背景面板(200×40)仅显示描述文本（无标题）。titleLabel/chalkLineImage 字段已删除。QuestTrigger 无 GameStart 模式，用大 triggerSize 覆盖出生点替代。注意：Tuanjie 编译管道偶报 com.unity.2d.tilemap GridPaintingState ScriptableSingleton 错误是包内部误报（空场景也出），验证编译以 MonoScript.GetClass() 加载成功为准。连死跳点已改：deathsBeforeSkip=3 后跳右侧第一个未激活的 Checkpoint（FindObjectsOfType 全场景扫描按 X 找最近右侧，跳过难关；无当前点用玩家实时位置做基准）。QuestTrigger 时序修复：QuestUI.Instance 为 null 时只跳过本帧不放弃（出生即在触发范围内的场景会因首帧未就绪而永不触发）。相机不跟到高处排查：相机Y钳制在 bounds.min.y+9~max.y-9，关卡最高平台超出 Bounds 上界时相机卡住——Bounds 上界=最高平台+半屏高(9)+余量(2)（scence1_2 已扩到 38.67）。scence1_2/scence1_3 骨架已建（复制 scence1_1 清理关卡保留 Game/Camera/SEM，Build Settings 已注册，出生点(0,-2)）；SceneTransition.prefab 可复用资产在 _Prefabs/Triggers/（目标场景名留空防误切）。


### User
- [2026-08-28 00:24:26] GitHub 用户名 ji-ren-exe（显示名 纪潇航）是同一个人。Git 提交中可能出现 "ji-ren-exe <ji-ren-exe@users.noreply.github.com>" 或 "纪潇航 <80156575+ji-ren-exe@users.noreply.github.com>" 两种作者信息，均为同一用户。当前项目远程仓库为 https://github.com/ji-ren-exe/SheWasAGirl.git。

### Feedback
- [2026-08-27 18:53:03] User preference: when AI sprite generation (generate_sprite with huoshan_seedream or frontier-game-design) produces multi-view reference sheets (FRONT/RIGHT/TOP/ISO labels with blue background) instead of single sprites, prefer drawing pixel art sprites programmatically via execute_csharp_script (Texture2D + EncodeToPNG) instead of retrying AI generation — it reliably produces clean, transparent, single-view pixel sprites.
- [2026-08-28 00:02:18] 完成任务后不要自动进行测试验证（不要自动进入 Play 模式、录制 GIF/MP4、截图或运行时诊断），除非用户明确要求"自主测试"。**Why:** 用户明确提出该要求，自动测试会增加不必要的耗时与打扰。**How to apply:** 代码/场景改动后，编译确认无报错即可交付；把运行验证留给用户，或仅在用户要求时执行。
- [2026-08-28 00:07:11] 每完成一项任务后，必须在交付说明中明确列出"需要重点测试的内容"（具体测试点、操作步骤、预期结果、易出问题的边界情况）。**Why:** 用户明确要求；由于不做自动测试，需要用户自行验证，清晰的测试清单能让用户高效确认改动是否正确。**How to apply:** 与"不自动测试"规则配合——交付时给出测试清单而不是自己去跑测试。
- [2026-08-28 01:37:58] execute_csharp_script 中 System.Drawing.Image 会被 Roslyn 解析为 UnityEngine.UIElements.Image 导致编译失败。解决方法：用强制类型转换 `(System.Drawing.Image)(System.Drawing.Image.FromStream(ms))`，同理 Graphics 用 `(System.Drawing.Graphics)(System.Drawing.Graphics.FromImage(bitmap))`。**Why:** Tuanjie 脚本环境默认引用了 UnityEngine 命名空间，Image/Graphics 类型名冲突。**How to apply:** 所有 System.Drawing 类型在 Roslyn 脚本中用全限定名 + cast。
- [2026-08-28 02:48:03] 修改 [SerializeField] 字段的代码默认值后，必须同时更新 Prefab 和场景对象上的序列化值，否则运行时读取的是旧的序列化值而非代码新值。用 execute_csharp_script 直接修改 Prefab/场景组件的序列化属性并 SaveAssets/SaveScene。**Why:** Prefab 序列化值优先级高于代码默认值，只改代码不生效。**How to apply:** 改完代码默认值后，用脚本同步更新 Prefab 和场景中所有同类型对象的序列化值。
- [2026-08-28 02:59:03] 不要在 PlayerController.Update 中每帧调用 SwitchHitbox 动态切换碰撞箱。**Why:** 每帧切换会破坏 Ducking 属性的 `this.collider == this.duckHitbox` 判断逻辑，且在 CheckGround 之前改变 collider 会导致地面检测异常，角色卡住或离地。**How to apply:** collider 只在 Init 和 Ducking setter 中设置一次，不同状态的碰撞箱差异应通过 Inspector 调整而非运行时动态切换。
- [2026-08-28 13:04:41] 2D 范围判定禁用 Bounds.Contains：玩家 Position 是 Vector2（z=0），触发器/交互物体 z 常为非 0（如 -7.395），triggerSize(Vector2) 转 Bounds 后 z 厚度为 0，Contains 三维判定永远 false。**Why:** DialogueTrigger 的 EnterRange 不触发排查了多轮，根因就是 Z 轴误判。**How to apply:** 所有 2D 范围判定用 Mathf.Abs 比较实现 X/Y 轴距离，明确忽略 Z。
- [2026-08-29 00:24:23] 用户提供自己修改过的美术源文件（如手动抠图、手动调整的场景布局）时：直接校验文件（MD5/尺寸/alpha）后替换项目资产并按新尺寸重排引用，绝不重新程序化生成替代版本；用户手动调整过的场景视为最终布局基准，只做增量修改。**Why:** 本会话用户两次用自己的成果覆盖 AI 生成的抠图和场景摆位，并明确说"这是我想要的结果"。**How to apply:** 收到"我做了X，请用该文件替换"或场景被用户手改后，走替换/保留流程而非重做。
- [2026-08-29 03:34:57] 2D 角色相关的视觉对象禁用父级继承缩放（三类案例）：① 残影 TrailSnapshot 白色方块问题——root.lossyScale 含 Z=0 传给残影 localScale（Vector2 隐式转 Vector3 也 Z=0），渲染异常+TrailSpriteEffect shader 白色输出=courses巨大白方块；修复：Add 只取 X/Y，Init 强制 localScale=new Vector3(x,y,1f)。② 耐力环 LineRenderer 同理被 Z=0 压扁。③ 根本原因：Player.Render 每帧设角色根 localScale=(-Abs(x),1,0)。**How to apply:** 任何跟随角色的动态视觉对象（残影/环/粒子）要么独立 Transform+世界坐标跟随，要么继承时强制 Z=1。场景一致性机制：PlayerRenderer.Reload 中 SyncSceneCameraFromMain 强制对齐相机 ShakeStrength=0.125（Main 调好值，原默认 1 抖动过强）。Main 与教程场景已对齐项：冲刺参数（DaughterStats）、相机抖动、耐力环、对话任务UI、ColliderVisualizer（两场景 Game 对象均有）。Main 旧独立 StaminaRing 已确认删除。
- [2026-08-29 07:40:35] 反射访问字段必须匹配访问级别：public 字段用 BindingFlags.Public|Instance，private 字段用 NonPublic|Instance——用错标志 GetField 返回 null 且无报错，?. 链式调用静默跳过，配合固定文案 Debug.Log 会造成"功能生效"的假象。**Why:** EnergyOrb 二段跳恢复失效排查：airJumps 是 public 但用了 NonPublic，null 静默失败+日志照打。**How to apply:** 反射写法 airField?.SetValue 后的日志必须输出读取回的实际值（如 ctrl.airJumps）做验证；写反射前先看目标字段声明。
- [2026-08-29 16:04:52] 场景摄像头两种模式约定：①跟随角色=默认加 Bounds（LevelBoundsUtility 实时计算）；②固定镜头=恰好包含整个背景图片（lockCamera=true, orthoSize 按背景包围盒+宽高比自适应）。用户给出"摄像头固定"或"摄像头跟随"时按此规则配置。
- [2026-08-29 20:41:26] 运行时不要用 GameObject.Find 查找非激活对象，也不要用 FindObjectsOfType<Transform>(true) 然后 SetActive(true) 来激活它们——非激活对象的 Start() 从未被调用过，激活后时序不确定（协程等 Canvas 可能卡住）。**Why:** DoorTransition_To2_4 方案花了大量轮次排查：GameObject.Find 找不到非激活对象 → 改用 FindObjectsOfType(true) 遍历能激活但 Start 提示 UI 不弹出（协程延迟 + Canvas 时序 + hintRoot 初始 null 跳过 ShowHint）→ 最终用户建议改为运行时动态创建新组件。**How to apply:** 需要"事件后新增交互触发器"时，直接在回调中 new GameObject + AddComponent 创建独立组件（如 DoorOpener），不要预设隐藏对象再激活。独立组件 Start 同步初始化，不依赖时序，FindObjectOfType<Canvas> 找不到就自建。

### Project
- [2026-08-27 18:53:07] Project is a 2D side-scrolling platformer (Celeste-style) named "频率1987 / She Game", built on Tuanjie 1.10.1 (Unity 2022.3.62). Migrated from 2D-Platform-Controller-main (Unity 2021.3.15f1). Code namespaces: Myd.Platform, Myd.Platform.Core, Myd.Common. Player controller uses custom position system (no Rigidbody2D on player), so interactions (pickup, rope climb) use distance-based detection, not Unity trigger callbacks.
- [2026-08-28 00:40:34] Tuanjie TextureImporter 序列化路径是 m_SpriteSheet.m_Sprites（不是 m_SpriteSheet.sprites）。用 SerializedObject 切片 Multiple Sprite 时必须用正确路径，否则 FindProperty 返回 null。
- [2026-08-28 03:22:21] 角色属性已从 Constants 全局参数抽离为 ScriptableObject（CharacterStats）。女儿属性配置在 Assets/Resources/DaughterStats.asset，Player.Reload() 时加载并调用 ApplyToConstants() 同步到 Constants。未来创建母亲角色只需新建 MotherStats.asset 配置不同属性。角色属性包括：移动速度、跳跃、二段跳次数、冲刺、攀爬耐力、墙跳等。
- [2026-08-28 13:04:32] 对话系统位于 Assets/ProPlatformer/_Scripts/Dialogue/：DialogueData（ScriptableObject，气泡列表含头像/文本/时长/speakerId，duration<=0 则按空格推进）、DialogueManager（场景单例，气泡UI+打字机效果，bubbleSound 气泡音效先 Stop 再 PlayOneShot 防重叠；按 speakerId 解析气泡跟随目标：0=玩家、1+=场景 DialogueSpeaker）、DialogueTrigger（4种触发模式：GameStart/EnterRange/KeyInRange/Condition）、InteractableObject（靠近按空格/E交互触发对话，selfAsSpeaker 开关可让气泡跟随物品自身）、DialogueSpeaker（挂场景NPC上，编号与气泡 speakerId 对应，气泡出现在 NPC 旁且朝向玩家一侧）。注意：项目 Coroutine 类型冲突需用 UnityEngine.Coroutine；Color 无 orange 常量需 new Color(1f,0.5f,0f)；中文文本用 LegacyRuntime.ttf 可能显示方块，如乱码需换中文字体。开场对话资产在 Assets/ProPlatformer/_Arts/Dialogue/。

- [2026-08-28 12:48:58] 对话触发排查要点：EnterRange 模式判定用的是 Player.Position（角色脚底），触发范围是以触发器中心为基准的矩形（triggerSize），玩家脚底必须真正进入矩形才触发。空格键同时是跳跃键——气泡 duration=0 时需按空格推进，会导致角色跳跃脱离，排查对话是否触发时应先把 duration 改为正数。触发器放在高处（需爬绳到达）时，攀爬中位置经过范围区间理论上可触发，但若耐力不足以到达该高度则永远无法进入范围。
- [2026-08-28 13:20:49] 冲刺残影系统：TrailSnapshot 残影生成时必须用 renderer.transform.root.lossyScale（根对象完整世界缩放含翻转负号），不能用子物体 localScale——角色翻转在 PlayerRenderer 根物体上（scale.x = -Abs * Facing），Sprite 子物体 localScale 恒为正，残影会反向。拖尾参数：生成间隔 0.04s（DashState Update 循环重置 DashTrailTimer）、残影存活 0.45s（PlayerRenderer.Trail 传 duration）、残影池上限 64 个（SceneEffectManager.snapshots）。交互键已统一为 E 键/手柄 JoystickButton2（Xbox X键），空格仅用于跳跃和对话推进；InteractableObject 靠近时在物体头顶显示"按 E 交互"提示条（挂在 DialogueManager Canvas 上）。
- [2026-08-29 20:58:57] 任务系统位于 Assets/ProPlatformer/_Scripts/Quest/：QuestData（ScriptableObject，questId/title/description）、QuestUI（场景单例，运行时由 PlayerRenderer.EnsureGlobalUI 自动创建）、QuestTrigger（玩家进范围切任务，紫色 Gizmo 框，X/Y 判定规避 Z 轴陷阱，无 GameStart 模式——用大 triggerSize 覆盖出生点替代）。任务资产在 Assets/ProPlatformer/_Arts/Quest/。QuestUI 现样式（2026-08-29 改版）：无标题，无黑板贴图，纯深色半透明背景面板(200×40px) + 浅粉笔色描述文本(fontSize=11, BestFit 8~11)，左上角锚定，切换时淡出→换文本→淡入 0.4s。DialogueManager 新增 StopDialogue() 公开方法（停协程+隐藏气泡+停音效），供 DoorOpener 切场景前调用。

- [2026-08-29 00:24:28] 石砖平台瓦片资产约定：贴图在 Assets/ProPlatformer/_Arts/Tiles/StoneBrick/（Fill 32×32 无缝填充 / Block 128×96 border=32 / Single 32×32 独立方块），Prefab 在 Assets/ProPlatformer/_Prefabs/Platforms/（Block=4×3瓦片、Platform=同sprite缩为2行、Fill、Single）。规格：32px@25PPU=1.28 世界单位/瓦片（与角色 25PPU 像素密度一致，Celeste 比例），Point 过滤，绘制模式 Tiled+Continuous（9-slice 感知平铺：顶/底条带水平重复、侧边垂直重复、四角固定），推荐尺寸取 1.28 倍数。碰撞箱只覆盖砖体+2px 密实泥（排除扇贝垂边），Block=（5.12,3.2）offset(0,0.32)。Tuanjie 坑：SpriteTileMode 无 Individual 成员；TextureImporter 无 spriteMeshType 公开属性，需用 SerializedObject 顶层属性 m_SpriteMeshType=0（FullRect）设置，否则 Tiled 平铺出警告。砖纹网格：节距16×8、错缝偏移8、1px 灰缝。同管线年代变体：1990s/Brick1990s_*（红砖16×8错缝）与 2010s/Ceramic2010s_*（白釉瓷砖8×8、错缝4），Prefab 同目录各4件（Block/Platform/Fill/Single），场景 CeramicBlock_2010s/BrickBlock_1990s 已换用。

- [2026-08-29 02:33:43] 平台碰撞箱自动同步：Myd.Platform.PlatformColliderSync（Assets/ProPlatformer/_Scripts/Level/PlatformColliderSync.cs，[ExecuteAlways]）已挂到全部 4 个 StoneBrick Prefab 及 1990s/2010s 年代变体 Prefab。Rect 工具拉伸 SpriteRenderer.size 时自动重算 BoxCollider2D：size=(w, h-bottomInset)、offset=(0, bottomInset/2)，bottomInset：Block/Platform=0.64、Fill=0、Single=0.48。组件 OnEnable/OnValidate/编辑器 Update 三重同步，场景加载时也会自动修复实例碰撞箱；未用 BoxCollider2D.autoTiling（其按完整精灵矩形含泥土垂边）。新建平台 Prefab 时挂此组件即可。

- [2026-08-28 21:49:17] 双年代街道素材（AI生成）：背景在 Assets/ProPlatformer/_Arts/Backgrounds/（Shared/Far_Day、Far_Dusk 白天黄昏共享远景 2048×572 已无缝化；1990s/Mid、Fore 与 2010s/Mid、Fore；原图在 _Raw/）。平台方块在 _Arts/Tiles/1990s/Tile_Brick_A-C 与 2010s/Tile_Ceramic_A-C（128×128 四方连续，25PPU Point），对应平台 Prefab 在 _Prefabs/Platforms/BrickBlock_1990s、CeramicBlock_2010s（Tiled 绘制 + PlatformColliderSync，默认1.28可拉伸），TilePalette 在 _Arts/Tiles/Palettes/。90砖块剥落白灰斑块在拼接时可能轻微可见（接缝差约35），必要时轮换 A/B/C 三种打破规律。
- [2026-08-29 00:24:17] 视差场景 Assets/ProPlatformer/Scenes/Parallax2010s.unity：2010s 街景测试关卡（Game/Level/Camera/Bounds 完整接线，出生点相机 x∈[-5,15]）。ParallaxLayer（_Scripts/Background/）为增量式：factor 1=锁相机(最远) 0=世界固定(最近)，画面滚动速度=(1-factor)×相机速度；层可随时手动摆位，退出 Play 自动还原。实测 Sky f=0.9→0.1×、Mid f=0.68→0.32×。中景=用户手动抠图 Mid_2010s_v2_keyed.png（1280×549，实心行145~376，实心底锚定路面 y=-2，5联排覆盖[-28,36]）；天空=SkyGrad_2010s 渐变背板+Sky_2010s_feather 羽化云条（程序抠图备份在 Mid_2010s_v2_keyed.png.autobackup.png）。关键约束：玩家可行走的地面（带碰撞+Ground）必须固定于世界(1×)否则碰撞错位；慢速装饰路面需无碰撞副本。
- [2026-08-29 00:24:26] Tuanjie 编辑器脚本 API 陷阱（补充）：无 EditorUtility.GetActiveScenePath（改用 EditorSceneManager.GetActiveScene().path）；导入后的贴图 Texture2D 不可 GetPixels32（需 File.ReadAllBytes+LoadImage 到临时纹理再读像素）；Roslyn 脚本 StringBuilder 需显式 using System.Text。程序化洪填抠图经验：容差42 四邻洪填 + 按实心行比例截断防泄漏，人物/建筑边缘平滑图建议用户手动抠图更干净。
- [2026-08-29 01:55:04] 角色精灵帧修复记录：DaughterRun_00 曾是 GIF 转图集残缺帧（不透明 12.3%、高 52.9px），导致跑步循环每 0.67s 规律性闪烁——已用第 1 帧像素覆盖首格+修正切片 rect(3,0,44x69) 修复（现 61.6%）。同图集 DaughterRun_04~07 曾共享重复 spriteID（历史切片工具缺陷），已改 meta 分配唯一 ID。诊断方法：文件直读像素（File.ReadAllBytes+LoadImage）逐切片统计不透明占比即可定位空帧/残帧。女儿三套图集均 8 帧@25PPU：Run 步进 49px 高 70、Idle 步进 35px 高 70、Jump 步进 ~38px。
- [2026-08-29 14:57:08] 跳跃音效三次修复：① 时机——原在「动画切入 Jump」时播，走出平台坠落也触发；改为离地瞬间检查 Speed.y>0.1 才播。② 根因——PlayOneShot 打在脚步声循环源上，被 Pause 的 AudioSource 上 PlayOneShot 无声且延迟补播；修复：Awake 增加独立 oneShotAudioSource。③ 第三次（2026-08-29）：滞空动画改为「一律播奔跑图」后 AnimState.Jump 永不被赋值，旧判定 `currentAnim == AnimState.Jump` 永不满足→音效消失；修复：音效触发与动画状态解耦，改用物理落地状态 `wasOnGroundLastFrame && !onGround`（起跳）/ `!wasOnGroundLastFrame && onGround`（落地）。高落差落地音同理从 `wasInAir` 改为 `wasOnGroundLastFrame`。**教训：音效/特效不要绑定到可能被改动的动画状态枚举，应绑定到物理状态（地面/离地）。oneShotAudioSource 独立源，IsOnGround/GetVerticalSpeed 通过反射访问。

- [2026-08-29 02:33:43] 输入系统（GameInput.cs，Legacy Input，activeInputHandler=0）：VisualButton 三参构造（键盘键码, 手柄JoystickButtonN, 缓冲时间），Jump=空格/A(0)、Dash=K/B(1)、Grab=J/Y(3)，移动走 Horizontal 轴（同名轴自动合并摇杆）。GameInput.UsingGamepad 跟踪最近输入设备（键盘键或摇杆倾斜>0.5 时上报），IsGamepadConnected 查物理连接。WorldSpaceHint（_Prefabs/UI/WorldSpaceHint.prefab）双文案字段 hintText/hintTextGamepad，按 UsingGamepad 全场景整套切换；TMP 中文字体已全局配置（Assets/Codely/Fonts/NotoSansSC-Regular SDF.asset + NotoSansSC-Outline.mat，Dynamic 图集运行时补字形，已入全局 fallback）。教程场景 Assets/Scenes/scence1_1.scene（用户手搭+我布的五段关卡：行走/跳跃台阶/冲刺沟7u/蹬墙跳竖井2.56宽/二段跳Δ4.8，6金牌+5提示双文案）。注意场景文件是 .scene（Tuanjie）非 .unity。
- [2026-08-29 02:34:30] 尖刺与复活点系统：Spike（_Scripts/Level/Spike.cs，X/Y 矩形判定 hitSize/hitOffset，红色 Gizmo，碰到玩家调 CheckpointManager.RespawnPlayer，无碰撞体不挡路）、Checkpoint（_Scripts/Level/Checkpoint.cs，玩家进范围激活，青色 Gizmo 框+白色复活点球标记，activeVisual 可选激活视觉，respawnOffset 复活偏移）、CheckpointManager（挂 Game 对象，跟踪 currentCheckpoint，RespawnPlayer 传送玩家+死亡冻帧 deathFreezeTime=0.25s+0.5s 防连触冷却；无激活点时回 Level.StartPosition）。Prefab 在 _Prefabs/Hazards/（Spike 用 small_metal_spike 贴图 hitSize=1.0x0.5、Checkpoint 无渲染纯逻辑体）。PlayerController.Respawn(position) 重置速度/状态/冲刺/耐力/相机。复活点只在激活的最近一个生效，教程场景 scence1_1.scene 已放示例（Spike_Ground_1/2、Checkpoint_1/2）。
- [2026-08-29 02:42:11] 按键映射现行版（2026-08-29 用户调整）：键盘=空格跳/X键交互/K冲刺/A D移动；手柄=A(0)跳/X(2)交互/RB右肩(5)冲刺/左摇杆移动。交互键已从E改为X（DialogueTrigger/InteractableObject 用 KeyCode.X+JoystickButton2，InteractableObject.hintText 默认与 Main.unity 序列化值已同步"按 X 交互"）。GameInput.Grab 已删除（抓墙改为按住朝墙方向键即抓，PlayerController.WantsGrab=HasStamina&&moveX==dir，无独立按键）。教程场景 Hint_Dash 手柄文案已改"空中按 RB 冲刺"。
- [2026-08-29 03:05:33] 全局UI自动创建机制（PlayerRenderer.Reload 中 EnsureGlobalUI）：任何场景角色加载时自动补齐 DialogueManager 和 QuestUI（判空后 new GameObject + AddComponent），无需手动放置。耐力环绑定机制：PlayerRenderer.Reload 中 AttachStaminaRing 创建独立 Transform 的 StaminaRing（不挂角色子物体！角色根物体每帧被设 localScale=(-Abs(x),1,0)，Z缩放=0 会把 LineRenderer 子物体压扁不可见、X负缩放会镜像），由 PlayerRenderer.OnDestroy 统一销毁，生命周期跟随角色。StaminaRingUI 每帧世界坐标跟随 Player.Position 并强制 localScale=one 中和继承缩放。场景对象（StaminaRing/DialogueManager/QuestUI/CheckpointManager）不随代码自动生成——新场景缺这些系统时检查此机制。尖刺判定已加大：hitSize=(1.0,1.2) hitOffset=(0,0.3) 覆盖跳跃掠过（原 0.5 高度太矮导致玩家脚底不命中）。SpikeEditor（Editor 文件夹）Inspector 一键生成一排尖刺。
- [2026-08-29 09:53:17] 场景切换系统：SceneTransition（_Scripts/Level/SceneTransition.cs）通用组件——targetSceneName 填 Build Settings 场景名、triggerSize 范围（橙黄 Gizmo+顶部"→场景名"标注）、additive 可选叠加；已升级为三阶段黑屏过渡：玩家进范围→黑屏淡入0.4s→加载目标场景→SceneFadeOut 跨场景淡出0.5s（等待角色就绪后才淡出）。坑：协程不跨场景，跨场景淡出必须挂 DontDestroyOnLoad 对象。Build Settings 已注册场景（2026-08-29 共6个）：SampleScene、scence1_1、scence1_2、scence1_3、scence2_1、Main（LoadScene 按名加载必须先注册）。教程场景已放示例 SceneTransition_ToMain@(28.16,9.8)（终点 Final_DoubleJump 上方）。动态创建的 DialogueManager 音效修复：bubbleSound 序列化字段为空时 Awake 自动 Resources.Load("TypeSound") 兜底（TypeSound.mp3 已复制到 Assets/Resources/，AssetDatabase 路径回退为辅）。主相机 cullingMask 陷阱：默认 Everything(-1) 会渲染 Post 层(26)的 Ripple quad → 冲刺结束白方块爆炸；Main 的正确值=119，已加入 SyncSceneCameraFromMain 自动对齐（检测到 -1 自动改 119）。冲刺动画：PlayerRenderer.IsDashing()（反射读 stateMachine.State==Dash），动画优先级=冲刺>空中>地面移动>站立，冲刺时播放跑步 GIF。


- [2026-08-29 05:44:07] scence1_2 视差背景定型：三层=BG_SolidBlack(-30, 近纯黑32x32缩放160x90兜底)→BG_Sky 晚霞(-20, Sky_Dusk.png 程序生成暮蓝→霞橙→暖黄渐变+14条柔云, 缩放14中心(24.5,20)覆盖Y-8~48, factor0.92含垂直)→BG_Mountain 山(-10, 用户山图裁黑边+顶24/底10羽化+纯黑色键抠图[原透明通道被压平成黑], factor0.65仅水平, 缩放2.7)。时差设计=晚霞远景+白日山中景。贴图在 _Arts/Backgrounds/scence1_2/，scence1_3 已复制同款三层。山图抠图验证法：像素级 alpha 统计（四角=0、顶部60行68.5%透明）比视觉分析工具可靠。天空不渲染排查：贴图PPU默认100（非25）导致实际高度只有估算1/4，天空Bounds半高必须大于相机Y+9。scence1_1 背景：Sky(-30,f0.92)+Mid_2010s×2(-20,f0.68,色(0.85,0.87,0.9,0.75)做模糊)；Hint提示背后加 Backdrop（深蓝黑半透明+青边，order-1，贴图 _Arts/UI/HintBackdrop.png）。任务UI已改居中+自适应字号：Tuanjie uGUI Text 的 Best Fit API 是 resizeTextForBestFit/resizeTextMinSize/resizeTextMaxSize（非标准 resizeFontForBestFit 名）。
- [2026-08-29 05:44:16] 遮挡关系速查（用户询问过）：2D渲染顺序由 Sorting Layer（大类，Project Settings→Tags and Layers 添加，列表顺序=绘制顺序）> Order in Layer（同层内大数盖小数）> Z坐标（前两者相同时才生效）。当前项目排序约定：黑底-30 → 天空-20 → 山/中景-10 → Hint底板-1 → 平台角色0 → 残影EffectForgeground层2 → 粒子/耐力环100。场景对象复制=选中Ctrl+D。生成新场景标准流程=复制 scence1_2.scene（含三层背景+系统骨架）→ Build Settings 注册 → 清关卡对象（保留Game/Camera/SEM/BG_*）→ 出生点(0,-2)。
- [2026-08-29 07:21:58] 实时 Bounds 系统（2026-08-29 定稿，取代手动扩 Bounds）：LevelBoundsUtility（_Scripts/Level/LevelBoundsUtility.cs）共享工具类——Compute(level) 以出生点+所有 SceneTransition 位置实时计算 Bounds（四周扩半屏+2余量），GetHalfView() 按相机当前 orthographicSize 动态取半屏（16:9 换算 halfW=halfH*16/9，拉远自动加宽）。接线：Level.RuntimeBounds 属性每次现算（旧序列化 Bounds 字段已 [Obsolete] 仅编辑器参考）、Game.Start 用 level.RuntimeBounds、PlayerController.CameraTarget 每帧调 Compute+动态半屏（Level 引用缓存避免每帧 FindObjectsOfType）。**相机"不跟随"标准排查**：出生点/关卡末端是否在 Bounds.min+半屏 ~ Bounds.max-半屏 内（正常半屏16/9，拉远24/12）——之前两次"相机不绑角色"（出生点X-16在钳制外、拉远后X>52超界）根因都是 Bounds 不含参考点。风粒子粉红色问题：程序化 ParticleSystem 必须手动赋 Sprites-Default 材质（无材质渲染洋红色，startColor 无效）。用户关卡已大幅扩展（scence1_3 切换点到X=267）。
- [2026-08-29 14:57:15] scence1_3 朝阳关卡内容（Bounds 部分已更新为实时系统见上条）：背景三层=BG_SolidBlack(-30)→BG_Sky_Dawn 朝阳天空(-20, Sky_Dawn.png 拂晓蓝紫→晨曦橙红→金光黄渐变+太阳光晕+光芒条纹+晨云, f0.95)→BG_Shan_x3 用户山图(-10, Shan.png 抠黑, 模糊色, f0.7)。用户场景后又大改：加了自己的天空图"1-4"(-25, f0.97基本不动, 位置27.8,-6.5缩放2.74)、关卡扩展到X=267、台阶重排(minY-3.9)、删了原终点切换器；后又扩展至X≈376并重建终点切换器(375.9,-31.2)指向scence2_1。WindPowerTrigger：疾风模式=相机9→12拉远+速度×1.35/跳跃×1.15+风粒子(右→左线条,发射源角色右侧+10,vel.x=-22,Stretch+lengthScale3.5,Sprites-Default材质)+常驻残影0.09s+BGM WindTheme.mp3播放60s。EnergyOrb：能量球拾取恢复airJumps=Max。**⚠ 待修复**：滞空动画改为「一律播奔跑图」本应仅限1-3，但代码（PlayerRenderer.UpdateAnimation）目前全局生效——用户明确说"仅限1-3修改了滞空时的动画"，需改为按场景开关控制，其他场景恢复跳跃GIF。



- [2026-08-29 07:40:24] scence1_3 近期迭代汇总（07:22 后）：①蹬墙跳场景禁用=WallJumpDisabler 组件（挂 Game 即关 NormalState.DisableWallJumpThisScene 静态开关，OnEnable/OnDisable/OnDestroy 三态复位；NormalState 跳跃分支检查 !DisableWallJumpThisScene）。②滞空动画全改奔跑图（含起跳瞬间，跳跃GIF完全不用）。③风痕全屏稀疏=发射源跟随相机右缘(halfW+1, 全高14)、rate 3.2/s+寿命1.2~2.2s+maxParticles 12 → 同屏3~5条、速度随机(-26~-16)+斜向(±0.8)不均匀、lengthScale 4.5。④能量球蓝色放大版48px贴图+scale1.6+OrbitParticles 子粒子(Sphere shape r0.35, orbitalY 1.2 环绕+radial 0.3 外扩+渐隐, Sprites-Default材质)。⑤WindEndTrigger(_Scripts/Level/)触发后调 WindPowerTrigger.Deactivate()=ZoomBackCamera 9恢复+DaughterStats.ApplyToConstants()属性还原+WindEffectRunner SetActive(false)+WindBGM Stop销毁；仅 IsWindActive 时可触发；部署于最右平台(54.7,-2.2)切换器之前。⑥FallRescueZone(挂 Game)=每2s缓存Ground层非Trigger碰撞体(记X中心/collider.bounds.max.y顶面/宽)，角色低于包络minY-2.5时传送到当前X列最高平台顶面+1.5(缝隙找最近平台X)，ctrl.Respawn+Speed清零。⑦**EnergyOrb二段跳不生效根因**：airJumps 是 public 字段但反射用 NonPublic 标志→GetField返回null静默跳过（Debug.Log照打造成假象）——修复用 Public|Instance；教训：反射访问字段先确认访问级别（public字段配Public标志、private字段配NonPublic），日志输出实际值验证而非固定文案。
- [2026-08-29 09:53:30] 本作为 SheNicest 比赛参赛作品，提交截止 2026-08-30。**Why:** 比赛硬截止日期。**How to apply:** 剩余时间内优先保证完成度与稳定性（关卡收尾、bug 修复、打包验证），避免大规模重构或高风险架构改动。

- [2026-08-29 09:53:22] scence2_1 已创建（2026-08-29，第二章首场景，Assets/Scenes/scence2_1.scene）：骨架=Camera(+Post)/SceneEffectManager/Game(Level+CheckpointManager+ColliderVisualizer)/BG_SolidBlack 纯黑底+出生地面 StoneBrick_Fill 8.96×1.28(顶面-2.25)，出生点(0,-2)。用户明确选择"纯黑底留空"——天空/中景由用户自己放图，勿代为生成背景；后续新场景背景先问用户。玩家对象无需放场景：运行时 Player.cs 从 Resources 实例化 PlayerRenderer。1-3 末端切换器(375.9,-31.2)已指向 scence2_1，链路已通。
- [2026-08-29 09:53:26] exec_editor_script 桥接坑（2026-08-29 scence2_1 创建时实测）：收到 [Unity notification: observation invalidated] 后脚本可能被重复分发——第一次执行已在 Unity 侧完整生效并保存，工具返回的却是第二次执行的早退报错（如"文件已存在，中止"），易误判为失败而盲目重跑造成循环。**How to apply:** 创建类脚本收到与预期不符的早退报错时，先只读检查文件/场景/Build Settings 实际状态（场景可能已成为活动场景）再决定补做；创建脚本保留 File.Exists 幂等守卫。
- [2026-08-29 14:57:23] DXT1 纹理压缩伪透明坑（2026-08-29 scence1_1 前景排查）：源图 100% 不透明（无 alpha 通道），但 TextureImporter alphaSource=FromInput + textureCompression=Compressed(DXT1) 的组合会让 GPU 采样产生半透明伪影——贴图边缘/颜色梯度区域出现伪透明像素，后方 Sky(order=-30) 穿透显现。修复：alphaSource=None + textureCompression=Uncompressed。诊断法：SpriteRenderer.color.a=1 但画面仍半透明时查 texture.format（DXT1 不支持 alpha 却可能产生伪影）；像素级 alpha 统计用 File.ReadAllBytes+LoadImage（导入后 Texture2D 不可 GetPixels32）。
- [2026-08-29 14:57:33] scence2_1 固定镜头机制（2026-08-29）：Level.cs 新增 lockCamera(bool)/lockTarget(Transform) 字段——勾选后 CameraTarget 直接返回 lockTarget 精灵包围盒中心（跳过 Bounds 钳制和 Y+2.2 偏移），相机不跟随角色。SyncSceneCameraFromMain 中新增自适应：lockCamera 场景加载时按 lockTarget 包围盒 + 实际 aspect 现算 orthoSize=max(半高, 半宽/aspect)，保证任何分辨率下完整容纳。scence2_1 配置：lockCamera=true, lockTarget=现在客厅(用户放的客厅背景图)。scence1_1 orthoSize 已改为 7（用户要求拉近，原 9）。
- [2026-08-29 19:44:41] 双角色切换系统（2026-08-29 scence2_3/2_4）：CharacterSwitchController（_Scripts/Level/）挂在 Game 上，Tab键/手柄左肩键(JoystickButton4)切换女儿↔母亲。切换逻辑：PlayerRenderer.SwitchCharacter(0/1) 换精灵帧 + Resources.Load("DaughterStats"/"MotherStats") ApplyToConstants + Level.lockTarget 指向对应背景(庭院新/庭院老) + PlayerController.Respawn 传送。PlayerRenderer 新增 ActiveCharacter 属性、motherIdleFrames/motherRunFrames/motherJumpFrames、motherSpriteScale=0.38、motherSpriteYOffset（母亲偏移，绝对定位 localPosition=(0,offset,0)）。母亲素材 MotherIdle.png(96×182)/MotherRun.png(98×152)/MotherJump.png(105×221) 在 _Arts/Textures/Player/Mother/（GIF转8帧@25PPU）。母亲奔跑缩放1.1（女儿0.8）。MotherStats.asset 在 Assets/Resources/。背景固定镜头（lockCamera=true），切换时 lockTarget 在两个背景间切换。CharacterSwitchController.SwitchCharacter() 已改为 public 供外部调用（ObjectRevealer/小游戏成功回调）。

- [2026-08-29 17:57:00] SceneTransition/DialogueTrigger/DialogueBubble 增强记录（2026-08-29）：①SceneTransition 新增 requireInteraction(按X键才触发+提示)、externalTriggerOnly(禁用Update自动检测，仅Trigger()方法可触发)、公开Trigger()方法供外部调用。②DialogueTrigger 新增 transitionAfterDialogue+targetTransition——对话播放完(WaitWhile IsPlaying)后调targetTransition.Trigger()切换场景。③DialogueBubble 新增 delayedSound/delayedSoundDelay/delayedSoundVolume——气泡出现后延迟播放音效(如电话铃声)，气泡结束时StopCoroutine。④HidePlayerOnLoad(_Scripts/Level/)——等待PlayerRenderer实例化后禁用所有SpriteRenderer，KeepHidden每0.5s检查防Respawn重载后可见。
- [2026-08-29 18:42:39] ⚠ 项目存在两个 PlayerRenderer.prefab：①Assets/ProPlatformer/_Prefabs/PlayerRenderer.prefab（编辑器中用户常编辑的）②Assets/Resources/PlayerRenderer.prefab（运行时 Player.Reload() 中 Resources.Load("PlayerRenderer") 实际加载的）。两者非同一文件！修改 PlayerRenderer 序列化字段（如 motherSpriteYOffset/motherSpriteScale）时必须同步到 Resources 版本，否则 Inspector 调节不生效。**How to apply:** 任何 PlayerRenderer.prefab 参数修改后，用脚本同步两个文件或直接编辑 Resources 版本。
- [2026-08-29 20:41:21] 修锁小游戏与物体激活系统（2026-08-29 scence2_3）：①ObjectRevealer（_Scripts/Level/）挂在小树上，按X键激活 targets 列表中的物体（大树+StoneBrick_Fill(2)），Start() 中 SetActive(false) 隐藏，交互后 SetActive(true)；提示UI延迟创建（协程等 Canvas 就绪）；交互后同时调 CharacterSwitchController.SwitchCharacter()。②LockPickingGame（_Scripts/MiniGame/）像素风修锁小游戏：三同心圆环各带缺口，A/D切换圆环 W/S旋转（手柄左摇杆同效），缺口对齐(<12°容差)→变绿→1.5s后回调 onSuccess。③老门交互链：InteractableObject(lockPickingAfterDialogue=true) → 按X对话"锁坏了，但是好像可以修"(DoorOld.asset, LeftBottom) → 对话结束 → 启动小游戏 → 成功后在 新门 上动态创建 DoorOpener 子对象。④新门交互：InteractableObject → 按X对话"锁坏太久了，早些来或许能修好"(DoorNew.asset, LeftBottom)。两门一次性。⑤DoorOpener（_Scripts/Level/）：独立组件，Start 同步创建提示UI（找不到 Canvas 则自建），Update 检测范围显示"按 X 开门"，按X后黑屏淡入0.4s+LoadScene+淡出0.5s。替代了之前的 DoorTransition_To2_4 方案（因非激活对象 SetActive 后 Start 时序问题被放弃）。⑥InteractableObject 新增 lockPickingMode(按X直接启动小游戏)、lockPickingAfterDialogue(对话后启动小游戏)两种模式。⑦DialogueData 新增 bubblePosition 枚举(Default/LeftBottom)控制气泡位置。⑧InteractableObject.Start 改为协程延迟初始化（等 DialogueManager/Canvas 就绪后再 BuildHintUI）。
- [2026-08-29 20:59:04] LockPickingGame 增强（2026-08-29）：①游戏期间 Time.timeScale=0 暂停角色移动，UI 用 unscaledDeltaTime 运行；成功/取消时恢复 Time.timeScale=1。②成功时 PlayUnlockSound() 程序化生成"咔嗒"金属碰撞音效（AudioClip.Create，无外部音频文件）。③成功回调中自动调 CharacterSwitchController.SwitchCharacter()。④DoorOpener.TransitionRoutine 切场景前调 DialogueManager.StopDialogue() 强制结束对话（停协程+隐藏气泡+停音效）。⑤scence2_3 进入时 QuestTrigger(triggerSize 50×50) 显示任务"按 Tab/左肩键 切换视角  探索两边的庭院"（Quest2_3.asset）。

### Reference
- [2026-08-29 18:42:37] 音效资产位于 Assets/ProPlatformer/_Arts/Audio/（FootstepGravel.mp3、JumpLight.mp3）。脚步声用 clip+loop+Play 循环播放（volume=0.3），停止/离地时 Pause() 保留进度、恢复跑步时 UnPause() 断点续播；跳跃声用 PlayOneShot 一次性播放。AudioSource 挂在 PlayerRenderer(Clone) 根对象（Awake 中动态添加，spatialBlend=0）。重物落地音效（heavyLandClip/jumpStartY/落差判定逻辑）已于 2026-08-29 删除。SceneEffectManager 的 JumpDust/DashLine 子对象曾丢失导致 MissingReferenceException，已重建并赋值。


